[pytest]
testpaths = tests
markers =
    slow: tests that fit a Prophet model (a few seconds each); deselect with -m "not slow"
filterwarnings =
    ignore::FutureWarning
    ignore::DeprecationWarning
