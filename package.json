{
  "name": "multi-agent-ai-business-consultant",
  "private": true,
  "description": "Root convenience scripts. The web app lives in web/, the API in backend/.",
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "dev:web": "npm --prefix web run dev",
    "lint": "npm --prefix web run lint",
    "build:web": "npm --prefix web run build"
  }
}
