import { LoadingScreen } from "@/components/LoadingScreen";

export default function Loading() {
  return <LoadingScreen title="Opening the workspace" subtitle="Preparing the agents and checking the API…" />;
}
